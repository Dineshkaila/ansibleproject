---
 - name: showing variables
   hosts: vlab
   vars:
     os: "linux"
     cloud: "aws"
     tools: "devops"
   tasks:
     - debug: msg="we will provide training in {{os}}"
     - debug: msg="lab experience in {{cloud}}"
     - debug: msg="basic knowledge in {{ tools}}"
